import numpy as np
from numpy import dtype
X = np.array(([0.4], [0.6]), dtype=float);
# Y = np.array(([0.2, 0.3], [0.6, 0.4]), dtype=float);
# Z = np.array(([1.0], [1.0]), dtype=float).T;
# a = np.dot(X, Y);
# b = a + Z;
# c = 1/(1+np.exp(-b));
print(X);